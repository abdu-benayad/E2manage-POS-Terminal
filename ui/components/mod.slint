// ============================================================================
// E2Manage POS Terminal - Components Module
// ============================================================================
// Central export file for all UI components
// Import components from here: import { Button, TextInput } from "components/mod.slint";
// ============================================================================

// Button components
export { Button, SmallButton, IconButton, LargeButton, QuickAmountButton } from "button.slint";

// Input components
export { TextInput, SearchInput, AmountInput, PinInput, SelectInput, TextArea } from "input.slint";

// Card components
export { Card, CardHeader, CardContent, CardFooter } from "card.slint";

// List item components
export { ListItem, ListSectionHeader, SelectableListItem, SwipeListItem } from "list_item.slint";

// Dialog components
export { Dialog, AlertDialog, LoadingDialog, InputDialog, ActionSheet } from "dialog.slint";
export { ShutdownDialog } from "shutdown_dialog.slint";
export { SyncSummaryDialog } from "sync_summary_dialog.slint";

// Badge and chip components
export { Badge, Chip, StatusDot, Tag, CountBadge, PriceTag } from "badge.slint";

// StatusBar components (Phase 4)
export { StatusBar, CompactStatusBar } from "status_bar.slint";

// Header components (Phase 4)
export { Header, PageHeader, SimpleHeader } from "header.slint";

// Side menu component (Phase 1 Enhancement)
export { SideMenu } from "side_menu.slint";

// Progress indicators (Phase 4)
export { ProgressBar, CircularProgress, LabeledProgress, StepProgress } from "progress_bar.slint";

// Numeric keypad components (Phase 5)
export { NumericKeypad, CompactKeypad } from "numeric_keypad.slint";

// PIN and amount display components (Phase 5)
export { PinDisplay, AmountDisplay, ChangeDisplay, TotalDisplay } from "pin_display.slint";

// Cashier tile components (Phase 7)
export { CashierTile, FaceLoginTile, MoreCashiersTile } from "cashier_tile.slint";

// Category tile components (Phase 15)
export { CategoryTile, AllCategoryTile, CategoryChip, CategoryData } from "category_tile.slint";
export { CategoryStrip, CategoryChipStrip, CategoryGrid, CategorySidebar } from "category_grid.slint";

// Product tile and grid components (Phase 16)
export { ProductTile, CompactProductTile, ProductListItem, ProductData } from "product_tile.slint";
export { ProductGrid, CompactProductGrid, ProductListView, RecentItemsStrip } from "product_grid.slint";

// Cart item and panel components (Phase 17)
export { CartItem, CompactCartItem, CartItemSkeleton, CartItemData } from "cart_item.slint";
export { CartPanel, CompactCartPanel, CartSummaryBar } from "cart_panel.slint";

// PLU pad component (Phase 3 — PLU Number Pad)
export { PLUPad } from "plu_pad.slint";

// Quantity pad component (Phase 3 — Quick Quantity Entry)
export { QuantityPad } from "quantity_pad.slint";

// Price check overlay component (Phase 3 — Price Check Mode)
export { PriceCheckOverlay } from "price_check_overlay.slint";

// Weight pad overlay component (Phase 3 — Scale/Weight Display)
export { WeightPad } from "weight_pad.slint";

// RTL layout components (Phase 40)
export { RtlText, RtlTextEnd, BackButton, ForwardButton, NavigationRow, ListRowWithChevron, DirectionIndicator, BidiContainer } from "rtl_layout.slint";
