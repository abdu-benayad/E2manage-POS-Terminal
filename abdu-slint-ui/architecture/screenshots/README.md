# Pattern verification screenshots

Visual evidence for load-bearing claims in `../segment-pattern.md` and `../segment-pattern-consultation.md`. The underscore-prefixed filenames mirror the source files in `components/_*.slint`.

Captured on Slint 1.14.1 (the version pinned by `Cargo.lock`).

## Files

| File | What it shows | Doc reference |
|---|---|---|
| `_segment-preview.png` | Segment regression suite — 7 verification scenarios rendered side-by-side: empty-cell zero-width (invariant 5), padding-h variations, align-h with cell wider than content, elide on constrained width, wrap on constrained width, font-family variations (Latin / Arabic / icon), typography variations. | `segment-pattern.md` → Segment, Appendix B. |
| `_segment-column-preview.png` | SegmentColumn regression suite — 5 scenarios: two-line composition (canonical use case), empty-secondary-collapses, vstack-spacing variations, SegmentColumn as one cell in a horizontal row, show:bool collapses the column to zero. | `segment-pattern.md` → SegmentColumn. |
| `_segment-column-vstack-verification.png` | 4-case vertical-stacking test confirming the horizontal-stretch-mediated bug class doesn't apply to vertically-stacked Segments. The diagnostic case is row 3: primary elide=true + secondary elide=false at 200px width — primary elides normally, does NOT collapse to zero. | `segment-pattern-consultation.md` → Q4 (empirical result), `segment-pattern.md` → SegmentColumn (justification). |
| `_badge-empty-wart-and-show-fix.png` | Early ad-hoc verification (from `/tmp/badge-empty-test.slint`): Badge wrapping an empty Segment **without** `show:bool` gating renders a 16px gold chrome strip (the wart); Badge with `show:false` renders zero-width invisible (the fix). | `segment-pattern.md` → Badge, "The show: bool convention". Superseded as a regression artifact by `_badge-preview.png` (which integrates the same scenario into the production library preview); kept here as the original empirical evidence. |
| `_badge-preview.png` | Badge regression suite — 4 scenarios. Scenario 1 (load-bearing): three-pill comparison showing the wart (`show=true` + empty content = visible chrome strip) next to the fix (`show=false` = zero contribution) next to a normal chip. Plus chrome variations, Badge as one inelastic cell in a horizontal row with a separate slack Rectangle (Invariant 7), and Badge wrapping a SegmentColumn for two-line chips. | `segment-pattern.md` → Badge. |
| `key-value-row-phase-4-ltr.png` / `key-value-row-phase-4-rtl.png` | Phase 4 verification — split-row structure with primary HorizontalLayout + conditional description row. Five scenarios per locale: full-row (description + status + label-icon + value-cluster + disclosure), description-with-minimal-trailing, single-line regression, value-unit-only / value-icon-only cluster variants, zigzag-stress-test (text-lg total emphasis + text-sm unit + label-icon + status + value-icon). Verifies the XNOR truth table for unit-position × Locale.rtl and that all cells optically align on a consistent baseline. | `key-value-row.md` → Composition + Split-row structure. |
| `key-value-row-phase-5.png` / `key-value-row-phase-5-rtl.png` | Phase 5 verification — sizing, density, divider. Six panels per locale: three side-by-side density cards (compact/default/comfortable), a 3-row list with show-divider on rows 1-2, a description-present row, a wrap-true row with a long label, and a locale-stability sanity row. Confirms locked-height single-line rows render at identical heights in both locales. | `key-value-row.md` → Locked row height, Density semantics. |
| `key-value-row-phase-8.png` | Phase 8 playground section. Shows the playground window with the KeyValueRow sidebar tile selected: sidebar (Button / IconButton / Toggle / Card / KeyValueRow), preview pane mounting a `KeyValueRow { label: "Total"; value: "12.50"; value-unit: "SAR"; }`, code-snippet panel below the preview, and the property panel on the right exposing all 18 public properties as LineEdit / CheckBox / ComboBox controls. Captured with `selected-section` temporarily defaulted to `"key-value-row"` so the screenshot lands on the mounted section directly; the shipped default in `playground.slint` is still `"button"`. | `key-value-row.md` → Acceptance criteria (playground section); IMPL plan → Phase 8. |
| `key-value-row-phase-7.png` / `key-value-row-phase-7-dark.png` | Phase 7 regression preview, top-portion sample. Shows the toolbar (LTR/RTL + Light/Dark pills) and sections 1-2 (Emphasis matrix, Value-tone matrix) in light and dark mode. The full preview is 2200px tall and contains 13 sections covering every acceptance criterion (Emphasis, Tone, Density, Unit-position × Locale, Description, Disclosure, Status dot, value-monospace, wrap, show-divider, Tooltip, Debug bounds, Dark mode). **Capture caveat**: slint-viewer only renders the on-screen portion of an oversized window, so the screenshots show only sections 1-2. Run `slint-viewer abdu-slint-ui/previews/key-value-row.slint` and scroll to verify the remaining sections interactively. | `key-value-row.md` → Acceptance criteria. |
| `key-value-row-phase-6.png` | Phase 6 verification — tooltip + debug-bounds. Four panels: baseline (no affordances), tooltip alone, debug-bounds alone, and both combined on the same row. **Capture caveat**: slint-viewer's winit backend in this environment did not respond to synthetic XTest motion events, so Frame 2 and Frame 4's tooltip popups were rendered by temporarily relaxing the popup's gate from `tooltip != "" && tooltip-touch.has-hover` to `tooltip != ""`. The relaxation was reverted before commit; the shipped code at `components/key-value-row.slint` (search for `tooltip-touch.has-hover`) gates on real hover. Hover behavior verified manually with a throwaway preview before Phase 7. | `key-value-row.md` → Tooltip TouchArea, Debug bounds. |

## Re-running the verifications

The first two screenshots came from preview files inside the library:
- `_segment-preview.png` → `slint-viewer abdu-slint-ui/previews/_segment.slint`
- `_segment-column-vstack-verification.png` → the verification source is in `/tmp/vstack-elide-test.slint` (not yet ported into the library; will land as `previews/_segment-column.slint` when SegmentColumn ships).

The badge test was an ad-hoc script (`/tmp/badge-empty-test.slint`) that will be folded into `previews/_badge.slint` when Badge ships. Until then, the screenshot is the canonical artifact for the wart-vs-fix claim.
